import { monolithOfUrl, MonolithOptions } from 'monolith';

console.log('TODO: Implement background page:', monolithOfUrl, MonolithOptions);
// monolithOfUrl(location.href, MonolithOptions.new())
//     .then(console.log)
//     .catch(console.error);
